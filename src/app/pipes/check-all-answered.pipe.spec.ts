import { CheckAllAnsweredPipe } from './check-all-answered.pipe';

describe('CheckAllAnsweredPipe', () => {
	it('create an instance', () => {
		const pipe = new CheckAllAnsweredPipe();
		expect(pipe).toBeTruthy();
	});
});
